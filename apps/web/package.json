{
  "name": "@kubi/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build:demo": "vite build --config vite.demo.config.ts"
  },
  "dependencies": {
    "@kubi/contracts": "workspace:*"
  }
}

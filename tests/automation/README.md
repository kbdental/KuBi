# automation suite

Generated from the same tables the runtime reads, so a new endpoint / transition
arrives already covered — or fails CI for lacking an entry (Phase 0.5 §18).

Phase 1: wired and running with **zero cases**. Cases are generated in the phase
that introduces the entities they cover. Hand-written authorization tests rot;
generated ones cannot.
